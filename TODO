TODO app
	List of tasks
	Loop that emits
	Mutate list (push)
	Store task list locally
twitter app

Observe collection add and remove mutation
Remove observations
Handler arguments

WANTS
	function that interpolates values
	function that persists variable locally
	color type
	for loop over static list ['one', 'two', 'three']
	for loop over twitter stream with jsonp
	function that persists variable remotely
